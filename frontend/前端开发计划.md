# 多客陪玩平台 - 前端开发计划

> 📅 计划制定时间：2026-05-23
> 🔖 版本：v1.0
> ✅ 状态：开发中

---

## 📋 一、已完成工作

### ✅ 1.1 API服务层建设

已创建完整的API服务层,包括:

**已有服务文件:**
- ✅ `authService.js` - 用户认证服务(已完善)
- ✅ `chatService.js` - 聊天服务
- ✅ `giftService.js` - 礼物服务
- ✅ `vipService.js` - VIP会员服务
- ✅ `albumService.js` - 相册服务
- ✅ `virtualUserService.js` - 虚拟用户服务
- ✅ `notificationService.js` - 通知服务
- ✅ `tagService.js` - 标签服务
- ✅ `trtcService.js` - 音视频通话服务
- ✅ `demandService.js` - 需求服务

**新增服务文件(2026-05-23):**
- ✅ `gamesService.js` - 游戏陪玩服务
  - 游戏分类查询
  - 陪玩师列表
  - 订单管理(发布/接单/完成/取消)
  - 陪玩申请

- ✅ `reserveService.js` - 预约服务
  - 时间段管理
  - 预约创建/确认/拒绝/取消/完成
  - 预约列表查询

- ✅ `circleService.js` - 圈子动态服务
  - 标签管理
  - 帖子发布/查询/详情
  - 评论系统
  - 点赞功能

- ✅ `projectService.js` - 项目管理服务
  - 项目CRUD
  - 订单管理
  - 统计分析

- ✅ `reportService.js` - 举报服务
  - 举报提交
  - 举报列表查询

- ✅ `uploadService.js` - 文件上传服务
  - 图片/音频/视频上传
  - 上传进度回调

**基础设施:**
- ✅ `common.js` - 完整HTTP请求封装
- ✅ 参数验证工具函数
- ✅ 统一错误处理
- ✅ Token自动管理
- ✅ 401自动跳转登录

---

## 🎯 二、开发计划

### 📅 阶段1: 用户认证功能 (高优先级)

**目标:** 实现完整的用户认证流程

**任务清单:**
1. ✅ 完善authService
   - ✅ 用户名密码登录
   - ✅ 手机验证码登录
   - ✅ 第三方登录(微信/QQ/抖音)
   - ✅ 用户注册
   - ✅ 密码重置
   - ✅ 关注/取关用户
   - ✅ 粉丝/关注列表
   - ✅ 发送短信验证码

2. 🔄 实现登录页面对接
   - 对接用户名密码登录API
   - 对接手机验证码登录API
   - 对接第三方登录API
   - 实现Token存储和刷新
   - 登录状态管理

3. 🔄 实现用户状态管理
   - Pinia用户信息Store
   - 登录状态持久化
   - Token自动管理
   - 路由守卫实现

**文件:**
- `src/store/user-info.js`
- `src/views/Login.vue`
- `src/router/index.js` (路由守卫)

---

### 📅 阶段2: 首页数据对接 (高优先级)

**目标:** 首页数据与后端API对接

**任务清单:**
1. 🔄 创建首页服务文件
   - Banner轮播数据API
   - 分类导航数据API
   - 推荐陪玩师列表API

2. 🔄 实现首页数据获取
   - 调用游戏分类API
   - 调用推荐陪玩师API
   - 实现下拉刷新/上拉加载更多
   - 骨架屏/加载状态

3. 🔄 优化首页交互
   - 搜索功能对接
   - 分类筛选对接
   - 推荐列表无限滚动

**文件:**
- `src/services/homeService.js` (新建)
- `src/views/Home.vue`
- `src/components/HomeBanner.vue`
- `src/components/HomeQuickNav.vue`
- `src/components/HomeRecommend.vue`

---

### 📅 阶段3: 聊天功能对接 (中优先级)

**目标:** 实现完整的聊天功能

**任务清单:**
1. 🔄 完善chatService
   - 聊天列表API对接
   - 消息列表API对接
   - 发送消息API对接
   - 消息撤回API对接
   - 消息已读API对接

2. 🔄 实现聊天页面
   - 聊天列表页面
   - 聊天详情页面
   - 消息发送/接收
   - 实时通信(Socket.IO)

3. 🔄 实现Socket.IO实时通信
   - 连接管理
   - 消息推送接收
   - 在线状态管理
   - 未读消息计数

**文件:**
- `src/services/chatService.js`
- `src/services/socketService.js`
- `src/views/ChatUsers.vue`
- `src/views/ChatRoom.vue`
- `src/store/chat.js`

---

### 📅 阶段4: 圈子动态功能 (中优先级)

**目标:** 实现动态发布和社交功能

**任务清单:**
1. 🔄 完善circleService
   - 标签列表API
   - 帖子列表/详情API
   - 发布帖子API
   - 评论/点赞API

2. 🔄 实现动态页面
   - 动态列表页面
   - 发布动态页面
   - 动态详情页面
   - 评论功能

3. 🔄 实现用户互动
   - 点赞/取消点赞
   - 评论/回复
   - 分享功能

**文件:**
- `src/services/circleService.js`
- `src/views/Activity.vue`
- `src/views/PublishPost.vue`
- `src/views/PostDetail.vue`

---

### 📅 阶段5: 商业功能对接 (中优先级)

**目标:** 实现充值、提现、钱包等商业功能

**任务清单:**
1. 🔄 完善支付服务
   - 充值套餐列表API
   - 创建充值订单API
   - 订单状态查询API
   - 微信支付对接

2. 🔄 实现充值页面
   - 充值套餐展示
   - 支付方式选择
   - 支付结果处理

3. 🔄 实现钱包页面
   - 余额查询
   - 交易记录查询
   - 提现功能

4. 🔄 实现VIP页面
   - VIP套餐列表
   - 会员权益展示
   - VIP购买流程

**文件:**
- `src/services/payService.js` (新建)
- `src/services/walletService.js` (新建)
- `src/views/Recharge.vue`
- `src/views/Wallet.vue`
- `src/views/VipCenter.vue`
- `src/views/Withdraw.vue`

---

### 📅 阶段6: 陪玩功能对接 (中优先级)

**目标:** 实现游戏陪玩完整业务流程

**任务清单:**
1. 🔄 完善gamesService (已创建)
   - 游戏分类查询
   - 陪玩师列表查询
   - 订单管理API

2. 🔄 实现游戏页面
   - 游戏分类页面
   - 陪玩师列表页面
   - 陪玩师详情页面

3. 🔄 实现订单流程
   - 发布订单(派单)
   - 接单大厅
   - 订单列表
   - 订单详情

4. 🔄 实现预约功能
   - 预约时间选择
   - 预约列表管理
   - 预约确认/取消

**文件:**
- `src/services/gamesService.js` (已创建)
- `src/services/reserveService.js` (已创建)
- `src/views/GameIndex.vue`
- `src/views/ServiceList.vue`
- `src/views/ServiceDetail.vue`
- `src/views/Paidan.vue`
- `src/views/MyOrder.vue`
- `src/views/MyReserve.vue`

---

### 📅 阶段7: 管理后台 (低优先级)

**目标:** 实现管理员功能

**任务清单:**
1. 🔄 管理员登录
   - 管理员认证API
   - Token管理

2. 🔄 管理后台页面
   - 用户管理
   - 订单管理
   - 提现审核
   - 数据统计

**文件:**
- `src/views/AdminLogin.vue`
- `src/views/AdminDashboard.vue`
- `src/views/AdminWithdraw.vue`

---

## 🔧 三、技术实现规范

### 3.1 API调用规范

```javascript
// ✅ 标准API调用
import authService from '@/services/authService'

try {
  const result = await authService.login(username, password)
  if (result.code === 200) {
    // 处理成功
    console.log('登录成功:', result.data)
  }
} catch (error) {
  // 处理错误
  console.error('登录失败:', error.message)
}
```

### 3.2 错误处理规范

```javascript
// 全局错误处理
import { RequestError } from '@/common/common'

try {
  await someAPI()
} catch (error) {
  if (error instanceof RequestError) {
    // 业务错误
    console.error(`错误码: ${error.code}, 错误信息: ${error.message}`)
  } else {
    // 网络错误
    console.error('网络连接失败')
  }
}
```

### 3.3 状态管理规范

```javascript
// Pinia Store使用
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import authService from '@/services/authService'

export const useUserStore = defineStore('user', () => {
  const userInfo = ref(null)
  const token = ref(localStorage.getItem('token'))
  
  const isLogin = computed(() => !!token.value)
  
  async function login(credentials) {
    const result = await authService.login(credentials)
    if (result.code === 200) {
      token.value = result.data.token
      userInfo.value = result.data.userInfo
      localStorage.setItem('token', token.value)
    }
    return result
  }
  
  return { userInfo, token, isLogin, login }
})
```

### 3.4 路由守卫规范

```javascript
// router/index.js
const publicRoutes = ['Login', 'Home', 'Search', 'Activity', 'PostDetail']

router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  const isPublicRoute = publicRoutes.includes(to.name)
  
  if (!isPublicRoute && !userStore.isLogin) {
    next({ name: 'Login', query: { redirect: to.fullPath } })
  } else {
    next()
  }
})
```

---

## 📊 四、开发进度

### 当前进度

| 模块 | 状态 | 完成度 |
|------|------|--------|
| API服务层建设 | ✅ 已完成 | 100% |
| 用户认证功能 | 🔄 进行中 | 20% |
| 首页数据对接 | ⏳ 待开始 | 0% |
| 聊天功能对接 | ⏳ 待开始 | 0% |
| 圈子动态功能 | ⏳ 待开始 | 0% |
| 商业功能对接 | ⏳ 待开始 | 0% |
| 陪玩功能对接 | ⏳ 待开始 | 0% |
| 管理后台 | ⏳ 待开始 | 0% |

**总体完成度: 15%**

---

## 🎯 五、下一阶段任务

### 立即执行任务:

1. **完善用户Store**
   - 创建完整的用户状态管理
   - 实现登录/登出逻辑
   - 实现Token自动刷新

2. **实现登录页面**
   - 对接authService API
   - 实现表单验证
   - 实现登录状态反馈

3. **创建首页服务**
   - 创建homeService.js
   - 实现Banner/分类/推荐API调用
   - 实现数据缓存

---

## 📝 六、注意事项

1. **Mock数据处理**
   - 所有页面目前使用Mock数据
   - 后续逐步替换为真实API调用
   - 保持UI/UX一致性

2. **错误处理**
   - 所有API调用必须try-catch
   - 显示友好的错误提示
   - 记录错误日志

3. **性能优化**
   - 使用Keep-Alive缓存组件
   - 实现图片懒加载
   - 优化列表渲染

4. **安全性**
   - Token存储在localStorage
   - 敏感信息加密
   - 防止XSS攻击

---

**📌 文档版本历史:**
- v1.0 (2026-05-23): 初始版本,完成API服务层建设

