# 多客陪玩系统 - 数据库表结构设计

> 更新时间：2026-06-06 | 数据库：MySQL + MongoDB | 框架：Sequelize + Mongoose | 表前缀 `xn_`

---

## 一、总览（42张表/集合）

### MySQL 表（39张）

| # | 表名 | 说明 | 核心字段 |
|:--|------|------|----------|
| 1 | `xn_admin` | 后台管理员 | username(唯一), password, nickname, avatar, email, phone, role_id, permissions, status, last_login_time/ip, create_time, create_admin_id |
| 2 | `xn_admin_role` | 管理员角色 | name(唯一), description, permissions, status, is_super, sort, create_time, create_admin_id |
| 3 | `xn_user` | 用户表 | id(BIGINT PK), username(唯一), nickname, avatar, mobile(唯一), password, open_id, money(gold), gift_money, score, lv, vip, vip_lv, sex, city, status, fans_num, dec, create_time |
| 4 | `xn_vip_package` | VIP套餐 | name, price, original_price, duration(days), level, hot, sort, status |
| 5 | `xn_vip_order` | VIP订单 | user_id, order_no(唯一), package_id, price, duration, pay_type, status(0待付/1已付/2取消), pay_no, create/pay_time |
| 6 | `xn_recharge_package` | 充值套餐 | name, price, coin, extra_coin, original_price, sort, status |
| 7 | `xn_order_chong` | 充值订单 | user_id, order_no(唯一), package_id, money, coin, pay_type, status, pay_no, create/pay_time |
| 8 | `xn_withdraw` | 提现记录 | user_id, money, pay_money, shouxufei(fee), type, bank, name, mobile, is_check, state, lailu, create_time |
| 9 | `xn_gift` | 礼物 | title, image, price, type, sort, status, hot, animation |
| 10 | `xn_gift_bag` | 礼包 | name, gifts(JSON), price, sort, status |
| 11 | `xn_gift_log` | 礼物日志 | from_user_id, to_user_id, gift_id, gift_name, price, num, total_price, room_id |
| 12 | `xn_red_packet` | 红包 | user_id, packet_no(唯一), total_money, total_num, remain_money/num, type, room_id, status |
| 13 | `xn_red_packet_log` | 红包日志 | packet_id, user_id, money |
| 14 | `xn_card` | 密卡 | card_no(唯一), card_pwd, face_value, coin_amount, status(0未用/1已用), use_time, use_user_id, expire_time, category, batch_no, remark |
| 15 | `xn_game` | 游戏/服务项 | name, icon, banner, sort, status |
| 16 | `xn_game_order` | 游戏订单 | order_no(唯一), user_id, companion_id, game_id, price, total_price, duration(hours), status(0待接/1已接/2进行中/3完成/4取消/5退款), pay_status, order_type |
| 17 | `xn_companion_profile` | 陪玩师档案 | user_id(唯一FK→user), nickname, avatar, gender, age, city, game_ids(JSON), game_names, price, tags, intro, images(JSON), voice_intro, order_count, good_rating, level, status(0待审/1通过/2拒绝) |
| 18 | `xn_post` | 帖子/动态 | user_id, content, images(JSON), video, type(0普通/1付费), tag_id, price, is_private, status, like/comment/view_count, location |
| 19 | `xn_post_like` | 帖子点赞 | (post_id+user_id 唯一), create_time |
| 20 | `xn_post_comment` | 帖子评论 | post_id, user_id, content, reply_to, reply_user_id, status |
| 21 | `xn_post_unlock` | 帖子解锁 | (post_id+user_id 唯一), price |
| 22 | `xn_user_follow` | 用户关注 | (user_id+target_user_id 唯一) |
| 23 | `xn_chat_log` | 聊天记录 | fromid, toid, content, type, time |
| 24 | `xn_chat_room` | 聊天房间 | user_id, target_user_id, room_id, last_message, last_time, unread_count |
| 25 | `xn_circle_tag` | 圈子标签 | name, icon, sort_order, status, create_time, update_time |
| 26 | `xn_report` | 举报记录 | user_id, target_user_id, target_type, target_id, reason, images, status(0待处理/1已处理), handle_result, handle_time |
| 27 | `xn_reserve` | 预约记录 | user_id, target_user_id, game_id, reserve_date, reserve_time, status |
| 28 | `xn_reserve_slot` | 预约时段 | (user_id+reserve_date+reserve_time 唯一), game_id, status |
| 29 | `xn_demand` | 需求单 | user_id, game_id, title, content, price, status, contact |
| 30 | `xn_call_record` | 通话记录 | from_user_id, to_user_id, room_id, call_type, call_no(唯一), duration, start_time, end_time, status |
| 31 | `xn_call_billing` | 通话计费 | call_id, user_id, duration_minutes, unit_price, total_cost, status |
| 32 | `xn_banner` | Banner管理 | title, image, link, type, sort, status |
| 33 | `xn_recommend` | 推荐位管理 | user_id, nickname, avatar, recommend_type(home/circle), start/end_time, is_top, sort_order, status |
| 34 | `xn_album_photo` | 相册照片 | user_id, image, sort, status |
| 35 | `xn_customer_service` | 客服账号 | userId, nickName, avatar, phone, email, status, online |
| 36 | `xn_virtual_user` | 虚拟用户 | name, avatar, gender, age, region, tags(JSON), intro, price_per_hour, online_status, is_recommend, status |
| 37 | `virtual_user_tag` | 虚拟用户标签 | name(唯一), code(唯一), description, category(personality/skill/style), icon, color, personality(JSON), expertise(JSON), communicationStyle, knowledgeScope(JSON), responseStrategy(JSON), promptTemplate, temperature(默认0.7), maxTokens(默认1024), priority, isDefault, status |
| 38 | `virtual_user_tag_relation` | 虚拟用户-标签关联 | (virtualUserId+tagId 唯一), isPrimary, customConfig(JSON) |
| 39 | `virtual_chat_history` | 虚拟用户聊天历史 | virtualUserId, userId, message, role(user/assistant/system), contextId |

### MongoDB 集合（3个）

| # | 集合名 | 说明 | 字段 |
|:--|--------|------|------|
| 40 | `chat_messages` | 实时聊天消息 | roomId(idx), senderId, senderName, senderAvatar, content, type, mediaUrl, duration, sendTime, isRevoked, createTime; 索引: roomId, senderId+sendTime, createTime |
| 41 | `user_sessions` | 用户会话列表 | userId(idx), peerId, peerName, peerAvatar, lastMessage, lastMessageType, lastMessageTime, unreadCount, updateTime; 索引: userId, (userId+peerId 唯一), updateTime |
| 42 | `notifications` | 通知消息 | userId(idx), type(1系统/2活动/3公告), title, content, data(Mixed), isRead, createTime; 索引: userId, userId+isRead |

---

## 二、数据库配置

```javascript
// MySQL (主库)
{
  database: "duoke_peer",
  host: "localhost:3306",
  user: "root",
  charset: "utf8mb4",
  pool: { max: 10(dev)/30(prod), min: 0(dev)/5(prod) }
}

// MongoDB (辅助库)
{
  uri: "mongodb://localhost:27017/duoke_peer"
}
```

---

## 三、外键关联关系

| 源表 | 外键 | 目标表 | 说明 |
|------|------|--------|------|
| xn_admin | role_id | xn_admin_role.id | 管理员所属角色 |
| xn_vip_order | user_id | xn_user.id | 订单所属用户 |
| xn_order_chong | user_id | xn_user.id | 充值用户 |
| xn_withdraw | user_id | xn_user.id | 提现用户 |
| xn_gift_log | from_user_id / to_user_id | xn_user.id | 赠送/接收双方 |
| xn_companion_profile | user_id | xn_user.id | 陪玩师=用户（一对一, belongsTo） |
| xn_game_order | user_id / companion_id / game_id | xn_user.id / xn_user.id / xn_game.id | 下单用户、陪玩师、游戏 |
| xn_post | user_id | xn_user.id | 发帖人 |
| xn_post_like | post_id / user_id | xn_post.id / xn_user.id | 帖子、用户 |
| xn_post_comment | post_id / user_id | xn_post.id / xn_user.id | 帖子、评论者 |
| xn_post_unlock | post_id / user_id | xn_post.id / xn_user.id | 帖子、解锁用户 |
| xn_user_follow | user_id / target_user_id | xn_user.id | 关注者、被关注者 |
| xn_report | user_id / target_user_id | xn_user.id | 举报者、被举报者 |
| xn_reserve | user_id / target_user_id | xn_user.id | 预约用户、陪玩师 |
| xn_reserve_slot | user_id | xn_user.id | 陪玩师 |
| xn_virtual_chat_history | virtualUserId / userId | xn_virtual_user.id / xn_user.id | 虚拟用户、真实用户 |
| xn_virtual_user_tag_relation | virtualUserId / tagId | xn_virtual_user.id / virtual_user_tag.id | 关联关系 |
| xn_recommend | user_id | xn_user.id | 被推荐用户 |
| xn_call_record | from_user_id / to_user_id | xn_user.id | 主叫、被叫 |

---

## 四、全局约定

1. **时间字段**：统一使用 `INTEGER(10)` 存储 Unix 时间戳（秒级），`xn_withdraw.create_time` 使用 DATE 类型。
2. **金额字段**：统一使用 `DECIMAL(10,2)`。
3. **JSON字段**：使用 `TEXT` 存储 JSON 字符串（数组/对象），在代码中 `JSON.parse/stringify` 处理。
4. **状态字段**：统一使用 `TINYINT(1)`。
5. **主键**：全部为 `INTEGER/BIGINT` 自增主键。
6. **驼峰表**：`virtual_user_tag`、`virtual_user_tag_relation`、`virtual_chat_history`、`xn_circle_tag` 使用驼峰字段名（如 `createTime`），其余使用下划线命名。
7. **timestamps**: 全部 39 个 MySQL 模型均设置 `timestamps: false`，手动管理时间字段。
8. **物理外键**：所有模型均**未**定义 Sequelize `references` 显式外键约束，外键关系为代码逻辑关联。

---

## 五、索引汇总

| 类型 | 数量 | 说明 |
|------|:----:|------|
| 单字段唯一索引 | 15 | Admin.username, AdminRole.name, User.username/mobile, Card.card_no, RedPacket.packet_no, VipOrder.order_no, GameOrder.order_no, OrderChong.order_no, CompanionProfile.user_id, CallRecord.call_no, CallBilling.call_id, VirtualUserTag.name/code |
| 组合唯一索引 | 5 | PostLike(post_id+user_id), PostUnlock(post_id+user_id), ReserveSlot(user_id+reserve_date+reserve_time), UserFollow(user_id+target_user_id), VirtualUserTagRelation(virtualUserId+tagId), UserSession(userId+peerId) |
| 普通索引 | 约80+ | 每个模型 2-5 个索引，覆盖常用的 where/order 字段 |
